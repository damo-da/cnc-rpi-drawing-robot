# Convert SVG picture to coordinates

## Setup
This project should work with Python 3. To install the dependencies, run `pip install -r requirements.txt`. 
For more information, Google search for `requirements.txt Python`.
